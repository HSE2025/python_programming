---
marp: true
header:
footer: ![height:40px](https://www.markslides.ai/image/credit.png)
paginate: false
class: normal
theme: default
size: 16:9
style: |
  pre {
    overflow: auto;
  }
---
# Неделя 6 — Словари и множества

> Сегодня: **словари как картотеки** (ключ → значение) и **множества как уникальные коллекции** для сравнительного анализа текстов.

---

## Зачем филологу словари и множества?

**Словари** — это структура для хранения связанных данных:

- Метаданные о произведениях (автор → произведения)
- Частотные словари (слово → количество)
- Индексы и конкордансы (слово → контексты)

**Множества** — это коллекции уникальных элементов:

- Словарь автора (все уникальные слова)
- Сравнение лексики разных авторов
- Быстрая проверка принадлежности

---

## Российский национальный корпус и частотные словари

![bg right:40%](https://cdn.markslides.ai/users/2662/images/hF9H9iWyrow5hKGGIf1kS)

**Частотный словарь** — фундамент корпусной лингвистики:

- Самые частотные слова русского языка
- Изменение частот по эпохам
- Ключевые слова автора/жанра
- Hapax legomena (слова, встречающиеся 1 раз)

---

# Часть 1: Словари (dict)

---

## Словарь как библиотечная картотека

**Аналогия**: Словарь — это картотека с подписанными карточками

```python
# Карточка о произведении
book = {
    "title": "Бедная Лиза",
    "author": "Карамзин",
    "year": 1792,
    "genre": "повесть"
}
```

- **Ключ** (key) — подпись на карточке: `"title"`, `"author"`
- **Значение** (value) — информация на карточке: `"Бедная Лиза"`, `"Карамзин"`

---

## Создание словарей

```python
# Литерал словаря
pushkin = {"name": "Александр Пушкин", "birth": 1799}

# Пустой словарь
catalog = {}
# или
catalog = dict()

# Из списка пар (кортежей)
items = [("title", "Евгений Онегин"), ("year", 1833)]
onegin = dict(items)
```

---

## Доступ и изменение

```python
book = {"title": "Бедная Лиза", "author": "Карамзин"}

# Чтение
print(book["title"])        # "Бедная Лиза"

# Изменение существующего значения
book["year"] = 1792

# Добавление новой пары
book["genre"] = "повесть"

# Удаление
del book["genre"]
```

---

## Безопасный доступ: метод .get()

```python
book = {"title": "Бедная Лиза", "author": "Карамзин"}

# Прямой доступ — может вызвать KeyError
# print(book["year"])  # ❌ KeyError: 'year'

# Безопасный доступ с значением по умолчанию
year = book.get("year", "неизвестно")
print(year)  # "неизвестно"

# Если ключ есть
author = book.get("author", "Аноним")
print(author)  # "Карамзин"
```

**Правило**: Используйте `.get()` когда ключ может отсутствовать.

---

## Навигация по словарю

```python
book = {"title": "Бедная Лиза", "author": "Карамзин", "year": 1792}

# Все ключи
print(book.keys())    # dict_keys(['title', 'author', 'year'])

# Все значения
print(book.values())  # dict_values(['Бедная Лиза', 'Карамзин', 1792])

# Все пары (ключ, значение)
print(book.items())   # dict_items([('title', 'Бедная Лиза'), ...])
```

---

## Итерация по словарю

```python
book = {"title": "Бедная Лиза", "author": "Карамзин", "year": 1792}

# По ключам (по умолчанию)
for key in book:
    print(key)

# По парам (ключ, значение) — рекомендуется
for key, value in book.items():
    print(f"{key}: {value}")

# Вывод:
# title: Бедная Лиза
# author: Карамзин
# year: 1792
```

---

## Частотный анализ: паттерн накопления

**Задача**: Посчитать, сколько раз встречается каждое слово

```python
text = "Бедная Лиза жила с матерью в одной деревне"
words = text.lower().split()

# Пустой словарь для частот
freq = {}

# Накопление частот
for word in words:
    freq[word] = freq.get(word, 0) + 1

print(freq)
# {'бедная': 1, 'лиза': 1, 'жила': 1, 'с': 1,
#  'матерью': 1, 'в': 1, 'одной': 1, 'деревне': 1}
```

---

## Паттерн частотного словаря (подробно)

```python
freq = {}  # Пустой словарь

for word in words:
    # Если слово уже есть — берём его счётчик
    # Если слова нет — берём 0
    current_count = freq.get(word, 0)

    # Увеличиваем на 1
    new_count = current_count + 1

    # Сохраняем обновлённый счётчик
    freq[word] = new_count

# Короткая запись:
freq[word] = freq.get(word, 0) + 1
```

---

## Сортировка частот

```python
freq = {'бедная': 1, 'лиза': 1, 'с': 3, 'матерью': 1, 'в': 2}

# Сортировка по частоте (по убыванию)
sorted_freq = sorted(freq.items(), key=lambda x: x[1], reverse=True)

print(sorted_freq)
# [('с', 3), ('в', 2), ('бедная', 1), ('лиза', 1), ('матерью', 1)]

# Топ-3 самых частых
for word, count in sorted_freq[:3]:
    print(f"{word}: {count}")
```

---

## Collections.Counter — автоматический счётчик

```python
from collections import Counter

text = "Бедная Лиза жила с матерью с пастушкой в одной деревне"
words = text.lower().split()

# Автоматический подсчёт
freq = Counter(words)

print(freq)
# Counter({'с': 2, 'бедная': 1, 'лиза': 1, ...})

# Топ-3 самых частых
print(freq.most_common(3))
# [('с', 2), ('бедная', 1), ('лиза', 1)]
```

---

## Counter: полезные операции

```python
from collections import Counter

text1 = "роза роза лилия"
text2 = "роза ландыш ландыш"

counter1 = Counter(text1.split())
counter2 = Counter(text2.split())

# Сложение частот
combined = counter1 + counter2
print(combined)  # Counter({'роза': 3, 'ландыш': 2, 'лилия': 1})

# Вычитание частот
diff = counter1 - counter2
print(diff)  # Counter({'лилия': 1, 'роза': 1})
```

---

# Часть 2: Множества (set)

---

## Множество как словарь автора

**Аналогия**: Множество — это список уникальных слов в лексиконе автора

```python
# Текст с повторами
text = "любовь любовь душа сердце душа"
words = text.split()
print(words)  # ['любовь', 'любовь', 'душа', 'сердце', 'душа']

# Уникальные слова (множество)
unique_words = set(words)
print(unique_words)  # {'любовь', 'душа', 'сердце'}

print(f"Всего слов: {len(words)}")         # 5
print(f"Уникальных: {len(unique_words)}")   # 3
```

---

## Создание множеств

```python
# Литерал множества
vocab = {"душа", "любовь", "сердце"}

# Из списка (автоматическое удаление дубликатов)
words = ["роза", "роза", "лилия", "роза"]
unique = set(words)
print(unique)  # {'роза', 'лилия'}

# Пустое множество — важно!
empty_dict = {}      # ❌ Это словарь!
empty_set = set()    # ✅ Это множество!
```

**Ловушка**: `{}` создаёт пустой **словарь**, не множество!

---

## Проверка принадлежности

```python
pushkin_vocab = {"душа", "любовь", "сердце", "печаль"}

# Быстрая проверка (O(1) — молниеносно)
print("душа" in pushkin_vocab)      # True
print("радость" in pushkin_vocab)   # False

# Добавление элементов
pushkin_vocab.add("тоска")

# Удаление элементов
pushkin_vocab.remove("печаль")  # KeyError если нет
pushkin_vocab.discard("несуществующее")  # Безопасно
```

---

## Множества: основные операции

```python
vocab = {"душа", "любовь", "сердце"}

# Добавить элемент
vocab.add("тоска")

# Удалить элемент
vocab.remove("любовь")     # KeyError если нет
vocab.discard("радость")   # Безопасно (не ошибка если нет)

# Очистить всё
vocab.clear()

# Длина
print(len(vocab))
```

---

## Операции множеств: теория множеств в действии

![bg right:35%](https://upload.wikimedia.org/wikipedia/commons/thumb/9/99/Venn0001.svg/300px-Venn0001.svg.png)

**Сравнение словарей разных авторов**:

- **Объединение** (`|`) — все слова обоих авторов
- **Пересечение** (`&`) — общие слова
- **Разность** (`-`) — уникальные для одного
- **Симметричная разность** (`^`) — только у одного

---

## Объединение: все слова вместе

```python
pushkin = {"душа", "любовь", "сердце", "печаль"}
lermontov = {"душа", "тоска", "печаль", "судьба"}

# Объединение: все слова, которые есть у кого-то
all_words = pushkin | lermontov
print(all_words)
# {'душа', 'любовь', 'сердце', 'печаль', 'тоска', 'судьба'}

# Альтернативный синтаксис
all_words = pushkin.union(lermontov)
```

---

## Пересечение: общие слова

```python
pushkin = {"душа", "любовь", "сердце", "печаль"}
lermontov = {"душа", "тоска", "печаль", "судьба"}

# Пересечение: слова, общие для обоих
common = pushkin & lermontov
print(common)  # {'душа', 'печаль'}

# Альтернативный синтаксис
common = pushkin.intersection(lermontov)

print(f"Общих слов: {len(common)}")
```

---

## Разность: уникальные слова

```python
pushkin = {"душа", "любовь", "сердце", "печаль"}
lermontov = {"душа", "тоска", "печаль", "судьба"}

# Разность: слова только у Пушкина
only_pushkin = pushkin - lermontov
print(only_pushkin)  # {'любовь', 'сердце'}

# Разность: слова только у Лермонтова
only_lermontov = lermontov - pushkin
print(only_lermontov)  # {'тоска', 'судьба'}

# Альтернативный синтаксис
pushkin.difference(lermontov)
```

---

## Симметричная разность: эксклюзивные слова

```python
pushkin = {"душа", "любовь", "сердце", "печаль"}
lermontov = {"душа", "тоска", "печаль", "судьба"}

# Симметричная разность: слова, которые есть только у одного
exclusive = pushkin ^ lermontov
print(exclusive)
# {'любовь', 'сердце', 'тоска', 'судьба'}

# Альтернативный синтаксис
exclusive = pushkin.symmetric_difference(lermontov)
```

---

## Таблица операций множеств

| Операция           | Символ | Метод                    | Описание            |
| -------------------------- | ------------ | ----------------------------- | --------------------------- |
| Объединение     | `a \| b`    | `a.union(b)`                | Все элементы     |
| Пересечение     | `a & b`    | `a.intersection(b)`         | Общие элементы |
| Разность           | `a - b`    | `a.difference(b)`           | Только в `a`       |
| Симм. разность | `a ^ b`    | `a.symmetric_difference(b)` | Только в одном  |

---

## Type-Token Ratio (TTR) — лексическое разнообразие

```python
text = "Я помню чудное мгновенье передо мной явилась ты"
words = text.lower().split()

total_words = len(words)           # Всего слов (tokens)
unique_words = len(set(words))     # Уникальных слов (types)

ttr = unique_words / total_words
print(f"TTR: {ttr:.2f}")  # 0.89 (высокое разнообразие)
```

**Интерпретация**:

- TTR близко к 1.0 — богатый, разнообразный словарь
- TTR близко к 0.3 — повторяющийся, простой словарь

---

## Коэффициент Жаккара — мера схожести

```python
text1 = "роза роза лилия тюльпан"
text2 = "роза ландыш тюльпан фиалка"

set1 = set(text1.split())  # {'роза', 'лилия', 'тюльпан'}
set2 = set(text2.split())  # {'роза', 'ландыш', 'тюльпан', 'фиалка'}

intersection = len(set1 & set2)  # Общие слова: 2
union = len(set1 | set2)         # Все слова: 5

jaccard = intersection / union
print(f"Коэффициент Жаккара: {jaccard:.2f}")  # 0.40
```

**Интерпретация**: 0 = нет общих слов, 1 = идентичный словарь

---

## Мини-практика (5 минут)

1. Создайте словарь `author` с ключами: `name`, `birth`, `death`
2. Добавьте ключ `works` со списком произведений
3. Посчитайте частоты слов в строке: `"любовь и душа и сердце и любовь"`
4. Создайте два множества слов из двух строк и найдите их пересечение

---

## Анти-капкан: словари

1. **Нехэшируемые ключи**: нельзя использовать списки как ключи

   ```python
   d = {["list"]: "bad"}  # ❌ TypeError
   d = {("tuple",): "ok"} # ✅
   ```
2. **`.get()` не изменяет словарь**: только возвращает значение

   ```python
   d = {}
   d.get("key", "default")  # не добавляет ключ!
   ```
3. **Пустой словарь vs пустое множество**:

   ```python
   {} == dict()   # ✅ пустой словарь
   set()          # ✅ пустое множество
   ```

---

## Анти-капкан: множества

1. **Множества неупорядочены**: нет индексов

   ```python
   s = {"a", "b", "c"}
   s[0]  # ❌ TypeError
   ```
2. **Множества изменяемы, но элементы должны быть неизменяемы**:

   ```python
   s = {[1, 2]}     # ❌ списки нельзя
   s = {(1, 2)}     # ✅ кортежи можно
   ```
3. **Производительность**: поиск в множестве/словаре O(1), в списке O(n)

   - Для больших коллекций множества намного быстрее!

---

## Чек-лист: словари

- [ ] **Создание**: `{}`, `dict()`, `{key: value}`
- [ ] **Доступ**: `d[key]` (прямой), `d.get(key, default)` (безопасный)
- [ ] **Изменение**: `d[key] = value`, `del d[key]`
- [ ] **Навигация**: `.keys()`, `.values()`, `.items()`
- [ ] **Итерация**: `for key, value in d.items():`
- [ ] **Паттерн частот**: `freq[word] = freq.get(word, 0) + 1`
- [ ] **Counter**: `from collections import Counter; Counter(words)`

---

## Чек-лист: множества

- [ ] **Создание**: `{элементы}`, `set(список)`
- [ ] **Пустое множество**: `set()` (не `{}`)
- [ ] **Проверка**: `element in my_set` (быстро!)
- [ ] **Операции**: `|` (объединение), `&` (пересечение), `-` (разность), `^` (симм. разность)
- [ ] **Методы**: `.add()`, `.remove()`, `.discard()`, `.clear()`
- [ ] **TTR**: `len(set(words)) / len(words)`
- [ ] **Жаккар**: `len(A & B) / len(A | B)`

---

## Итоги

- **Словари** — отображение ключ → значение (картотека, метаданные, частоты)
- **Безопасный доступ**: `.get(key, default)` предотвращает ошибки
- **Частотный анализ** — фундаментальная операция в DH
- **Counter** — автоматический подсчёт частот
- **Множества** — уникальные элементы (словарь автора)
- **Операции множеств** — сравнение лексики разных текстов
- **Метрики**: TTR (разнообразие), Жаккар (схожесть)

---

## Домашка 7.1 (30 минут)

**Создайте комплексный лингвистический анализатор**:

1. **Частотный анализ**: посчитайте частоты слов (вручную и через Counter), найдите топ-5 и hapax legomena
2. **Анализ словаря**: размер словаря, TTR, примеры слов разной длины
3. **Сравнительный анализ**: возьмите два отрывка, найдите общие/уникальные слова, коэффициент Жаккара
4. **Метаданные**: словарь словарей с информацией о каждом слове (длина, частота, позиция)
5. **Интерпретация**: объясните, что означают полученные метрики

Критерии: корректность (35%), использование словарей/множеств (25%), полнота (25%), интерпретация (15%)

---

## Что дальше?

**Неделя 8**: Вложенные структуры данных, JSON, работа с API
**Неделя 9**: Регулярные выражения для извлечения информации из текстов
**Неделя 10**: Pandas и табличные данные для больших корпусов

Увидимся на следующем занятии! 📚✨
