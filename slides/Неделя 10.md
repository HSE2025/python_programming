---
marp: true
header: 
footer: Неделя 10 — От Скрипта к Проекту
title: "Неделя 10"
paginate: true
class: normal
theme: default
size: 16:9
style: |
  pre {
    overflow: auto;
  }
---

<!-- _class: lead -->

# Неделя 10
## От Скрипта к Проекту: Профессиональный Python

**Организация кода • Работа с файлами • Обработка ошибок**

---

# Распространённые ошибки

```python
# Программа для анализа текста
f = open('dante.txt')
text = f.read()

def analyze(t):
    # Считаем слова
    return len(t.split())

if _name_ == "__main__":
    print(analyze)
```

**Задание:** Найдите ВСЕ ошибки! (1 минута)

---

## ❌ Ошибка #1: Нет Docstring

```python
def analyze(t):
    # Считаем слова  ❌ Это комментарий, не docstring!
    return len(t.split())
```

---

**Правильно:**
```python
def analyze(text):
    """
    Подсчитывает количество слов в тексте.

    Args:
        text (str): Текст для анализа

    Returns:
        int: Количество слов
    """
    return len(text.split())
```
---

## Стоп! А в чем разница: `#` vs `"""`?

Почему нельзя просто написать комментарий `#`?

**Это РАЗНЫЕ вещи!** 

---

## Комментарии (`#`) - Для программистов

**Комментарии - это заметки для ЛЮДЕЙ, которые читают код:**

```python
def count_words(text):
    # Разбиваем текст на слова по пробелам
    words = text.split()

    # Возвращаем количество слов
    return len(words)
```
---

**Комментарии:**
- Объясняют **КАК** работает код (реализацию)
- Помогают понять сложные места
- Python **игнорирует** их полностью
- **НЕ доступны** через `help()`
- **НЕ видны** в документации

**Аналогия:** Комментарии = заметки на полях книги

---

## Docstrings (`"""`) - для пользователей функции

```python
def count_words(text):
    """
    Подсчитывает количество слов в тексте.

    Args:
        text (str): Текст для анализа

    Returns:
        int: Количество слов
    """
    words = text.split()  # Реализация может быть сложной
    return len(words)     # но docstring описывает ЧТО, не КАК
```

---
**docstrings**
- Описывают **ЧТО** делает функция (интерфейс)
- Доступны через `help(функция)`
- Видны в автодополнении IDE
- Генерируют автоматическую документацию
- **Обязательны** для всех публичных функций

**Аналогия:** Docstring = аннотация на обложке книги

---

## Сравнение: Комментарий vs Docstring

**Комментарий `#`**
```python
def analyze(text):
    # Эта функция считает слова
    return len(text.split())
```

**Проверка:**
```python
>>> help(analyze)
Help on function analyze:
analyze(text)
    # НЕТ ОПИСАНИЯ!
```

**Результат:** Пустая документация

---

**Docstring `"""`**
```python
def analyze(text):
    """Подсчитывает слова."""
    return len(text.split())
```

**Проверка:**
```python
>>> help(analyze)
Help on function analyze:
analyze(text)
    Подсчитывает слова. ✅
```

**Результат:** Читаемая документация


---

## Правило: Что где использовать

**Docstring (обязательно!) - описывает интерфейс:**
```python
def calculate_ttr(text):
    """
    Вычисляет Type-Token Ratio (лексическое разнообразие).

    TTR = количество уникальных слов / общее количество слов

    Args:
        text (str): Текст для анализа

    Returns:
        float: Значение TTR от 0 до 1
    """
```
---

**Комментарии (опционально) - объясняют реализацию:**
```python
    # Приводим к нижнему регистру для правильного подсчёта
    words = text.lower().split()

    # Используем set() для получения уникальных слов
    unique = len(set(words))
    total = len(words)

    # Защита от деления на ноль
    return unique / total if total > 0 else 0
```

---

## Кулинарный рецепт 

**Docstring = Описание**
```python
def make_pasta():
    """
    Готовит пасту карбонара (4 порции).

    Args:
        None (используются стандартные ингредиенты)

    Returns:
        str: Готовое блюдо

    Время: 30 минут
    Сложность: Средняя
    """
```
---

**Комментарии = Пояснения в рецепте:**
```python
    # Важно: добавляйте яйца в тёплую, не горячую пасту
    pasta = cook_pasta()

    # Возьмите свежий сыр для лучшего вкуса
    cheese = grate_parmesan()
```

---

## Docstring как специальный атрибут

```python
def greet(name):
    """Приветствует пользователя."""
    return f"Привет, {name}!"

# Docstring хранится в атрибуте __doc__
print(greet.__doc__)
# Вывод: Приветствует пользователя.

# help() использует __doc__
help(greet)
# Help on function greet:
# greet(name)
#     Приветствует пользователя.

# VS Code показывает при наведении
greet(  # ← При вводе "(", VS Code показывает docstring
```

---

## Формат Docstring: Минимальный вариант

**Для простых функций достаточно короткого описания:**

```python
def count_words(text):
    """Подсчитывает количество слов в тексте."""
    return len(text.split())

def count_lines(text):
    """Подсчитывает количество непустых строк."""
    lines = [l for l in text.split('\n') if l.strip()]
    return len(lines)
```

**Минимум: одна строка с описанием функции функции**

---

## Плохие примеры Docstring

**Пример 1: Описывает КАК, а не ЧТО**
```python
def analyze(text):
    """
    Использует split() для разделения текста и len() для подсчёта.
    """  # ❌ Описывает реализацию!
```

**Правильно:**
```python
def analyze(text):
    """Подсчитывает количество слов в тексте."""  # 
```

---

**Пример 2: Бесполезное описание**
```python
def calculate_ttr(text):
    """Вычисляет TTR."""  # ❌ А что такое TTR?
```

**Правильно:**
```python
def calculate_ttr(text):
    """
    Вычисляет Type-Token Ratio (лексическое разнообразие).

    TTR = уникальные слова / все слова
    """  # ✅ Понятно!
```

---

Создайте функцию и проверьте разницу:

```python
# Вариант 1: ТОЛЬКО комментарий
def version1(text):
    # Считает слова в тексте
    return len(text.split())

# Вариант 2: ТОЛЬКО docstring
def version2(text):
    """Считает слова в тексте."""
    return len(text.split())

# Проверка:
help(version1)  # Что выведется?
help(version2)  # Что выведется?
```

---

## Итого: Комментарии vs Docstrings

| Характеристика | Комментарий `#` | Docstring `"""` |
|----------------|-----------------|-----------------|
| **Назначение** | Объясняет КАК | Описывает ЧТО |
| **Для кого** | Разработчик | Пользователь функции |
| **Доступность** | Только в коде | `help()`, IDE, docs |
| **Обязательность** | Опционально | **Обязательно** для функций |
| **Позиция** | Где угодно | Сразу после `def` |

**Правило:** Каждая функция ДОЛЖНА иметь docstring!

---

**Docstring пишем:**
- После `def функция():`
- В начале модуля (файла .py)

**Комментарии пишем:**
- Перед сложным блоком кода
- Для объяснения неочевидных решений
- Для TODO/FIXME заметок
- Для отключения кода при отладке

**НЕ пишем комментарии:**
- Для очевидных вещей: `x = 5  # присваиваем 5`
- Для дублирования кода: `print(text)  # печатаем текст`
- Вместо docstring!

---

## Работа с Файлами

```python
f = open('dante.txt')  # ❌ Две проблемы!
text = f.read()
```

**Проблемы:**
1. Нет `encoding='utf-8'` → кракозябры с кириллицей
2. Нет `with` → файл не закрывается

---

**Правильно:**
```python
with open('dante.txt', 'r', encoding='utf-8') as f:
  text = f.read()
```

---

## Но Почему `with open()`? Разбираемся!

**Вопрос студента:** "Зачем писать `with`? Почему нельзя просто `open()`?"

**Короткий ответ:** Файлы нужно закрывать, а `with` делает это автоматически!

**Давайте разберёмся подробно... **

---

## Как Python Работает с Файлами

**Когда вы открываете файл, происходит 3 вещи:**

```python
f = open('text.txt', 'r')
```

1. **Операционная система выделяет ресурс** (file descriptor)
2. **Python создаёт объект файла** (`f`)
3. **Устанавливается блокировка** - другие программы не могут изменять файл

**Проблема:** Эти ресурсы нужно **освободить** после использования!

```python
f.close()  # ❌ А если вы забудете?
```

---

## Что Происходит Без `close()`?

```python
# Плохой код
f = open('dante.txt', 'r')
text = f.read()
# Программа заканчивается... файл всё ещё открыт! 
```

**Последствия:**
1. **Утечка ресурсов** - ОС думает, что файл используется
2. **Блокировка файла** - другие программы не могут его открыть
3. **Данные не сохранены** - буфер записи не очищен
4. **Ограничение файлов** - ОС разрешает ~1024 открытых файла

**В большой программе:** открыли 1000 файлов → программа упала!

---

## Попытка #1: Закрывать Вручную

```python
# Лучше, но всё ещё плохо
f = open('dante.txt', 'r')
text = f.read()
f.close()  # ✅ Закрыли файл!
```

**Новая проблема:** А если произошла ошибка?

```python
f = open('dante.txt', 'r')
text = f.read()
result = analyze(text)  # ❌ Ошибка здесь!
f.close()  # Эта строка НИКОГДА не выполнится!
```

**Файл остался открытым!**

---

## Попытка #2: try-finally

```python
# Работает, но громоздко
f = open('dante.txt', 'r')
try:
    text = f.read()
    result = analyze(text)
finally:
    f.close()  # ✅ Выполнится ВСЕГДА, даже при ошибке
```

**Проблема:** Много кода для простой операции!

**Решение:** В Python есть **Context Manager** (менеджер контекста)

---

## Context Manager: `with` Statement

**Context Manager = автоматическое управление ресурсами**

```python
with open('dante.txt', 'r', encoding='utf-8') as f:
    text = f.read()
# Файл автоматически закрыт здесь!
```

**Что делает `with`:**

```python
# Псевдокод - как работает with
f = open('dante.txt', 'r', encoding='utf-8')
try:
    # Ваш код здесь
    text = f.read()
finally:
    f.close()  # ВСЕГДА выполняется
```
---

**Преимущества:**
- Файл закрывается автоматически
- Даже если произошла ошибка
- Меньше кода
- Невозможно забыть закрыть

---

```python
class FileManager:
    def __enter__(self):
        """Вызывается при входе в блок with"""
        print("Открываем файл...")
        self.file = open('text.txt', 'r')
        return self.file

    def __exit__(self, exc_type, exc_val, exc_tb):
        """Вызывается при выходе из блока with"""
        print("Закрываем файл...")
        self.file.close()

# Использование:
with FileManager() as f:
    text = f.read()  # Между __enter__ и __exit__
```

---

## Правило: ВСЕГДА используйте `with` для файлов

```python
# ❌ ПЛОХО
f = open('file.txt', 'r')
text = f.read()
f.close()

# ❌ ОЧЕНЬ ПЛОХО
f = open('file.txt', 'r')
text = f.read()
# Забыли закрыть!

# ✅ ОТЛИЧНО
with open('file.txt', 'r', encoding='utf-8') as f:
    text = f.read()
```

---

## А Что с `encoding='utf-8'`?

**Почему обязательно?**

```python
# БЕЗ encoding (использует системную кодировку)
with open('dante.txt', 'r') as f:  # ❌
    text = f.read()
# Windows: cp1251
# macOS: UTF-8
# Linux: зависит от настроек
```

**Результат на Windows:**
```
Ð'Ð¾Ð¶ÐµÑÑ‚Ð²ÐµÐ½Ð½Ð°Ñ ÐšÐ¾Ð¼ÐµÐ´Ð¸Ñ  ← кракозябры!
```

---

**С `encoding='utf-8'`:**
```python
with open('dante.txt', 'r', encoding='utf-8') as f:  # ✅
    text = f.read()
# Результат: Божественная Комедия ← правильно!
```

**Правило:** **ВСЕГДА** указывайте `encoding='utf-8'`!

---

## Main Guard

```python
if _name_ == "__main__":  # ❌ Одно подчёркивание!
    print(analyze)
```

**Правильно:**
```python
if __name__ == "__main__":  # ✅ ДВА подчёркивания с каждой стороны
    result = analyze(text)  # ✅ Вызываем функцию
    print(result)           # ✅ Печатаем результат
```

**Частые ошибки:**
- `_name_` вместо `__name__`
- Забыть двоеточие
- Печатать функцию, а не её результат

---

## Зачем Нужен `if __name__ == "__main__"`?

Что это значит? Зачем это писать каждый раз?

**Короткий ответ:** Чтобы код запускался только когда мы запускаем файл напрямую, а не при импорте!

---

## Как Python Выполняет Код

**Когда Python читает файл `.py`, он:**

1. **Выполняет ВСЕ строки сверху вниз**
2. **Запоминает (определяет) функции** (не вызывает их!)
3. **Запоминает (определяет) классы**
4. **Выполняет код вне функций** (прямо сейчас, в процессе чтения!)

---

**Пример:**

```python
# test.py
print("Эта строка выполнится СРАЗУ")

def greet(name):
    print(f"Привет, {name}!")

print("Эта строка тоже выполнится СРАЗУ")
greet("Анастасия")  # Эта тоже!
```

**Запуск:** `python test.py`
**Вывод:**
```
Эта строка выполнится СРАЗУ
Эта строка тоже выполнится СРАЗУ
Привет, Анастасия!
```

---

## Проблема: Импорт Выполняет Код!

**`greetings.py`:**
```python
def greet(name):
    print(f"Привет, {name}!")

print("Модуль greetings загружен!")
greet("Тестовый пользователь")
```

**`main.py`:**
```python
import greetings  # ❌ Что произойдёт?

greetings.greet("Анастасия")
```
---
**Запуск:** `python main.py`
**Вывод:**
```
Модуль greetings загружен!      ← Из greetings.py
Привет, Тестовый пользователь!  ← Из greetings.py (НЕЖЕЛАТЕЛЬНО!)
Привет, Анастасия!              ← Из main.py (то, что мы хотели)
```

**Проблема:** При импорте выполнился весь код из `greetings.py`!

---

**Исправленный `greetings.py`:**
```python
def greet(name):
    print(f"Привет, {name}!")

if __name__ == "__main__":
    # Этот код выполнится ТОЛЬКО если запустить файл напрямую
    print("Модуль greetings загружен!")
    greet("Тестовый пользователь")
```

**Теперь запуск `python main.py`:**
```
Привет, Анастасия!  ← Только то, что мы хотели!
```

**А если запустить `python greetings.py` напрямую:**
```
Модуль greetings загружен!
Привет, Тестовый пользователь!
```

---

## `__name__` - это специальная переменная Python

**Значение зависит от того, КАК запущен файл:**

```python
# test.py
print(f"__name__ = {__name__}")
```

**Вариант 1: Прямой запуск**
```bash
python test.py
```
**Вывод:**
```
__name__ = __main__
```
---

**Вариант 2: Импорт из другого файла**
```python
# main.py
import test
```
**Вывод:**
```
__name__ = test
```

**Правило:** Если файл запущен напрямую → `__name__` = `"__main__"`

---

## Зачем это нужно: реальные примеры

**Пример 1: Тестирование функций**
```python
# text_utils.py
def count_words(text):
    return len(text.split())

if __name__ == "__main__":
    # Тестируем функцию во время разработки
    print(count_words("hello world"))  # 2
    print(count_words(""))              # 0
```

**Запуск тестов:** `python text_utils.py`
**Использование в проекте:** `from text_utils import count_words`

---

## Пример 2: Демо-Код

```python
# file_utils.py
def read_file(path):
    with open(path, 'r', encoding='utf-8') as f:
        return f.read()

if __name__ == "__main__":
    # Демонстрация работы модуля
    print("=== Демо: чтение файла ===")
    content = read_file('example.txt')
    print(content)
```

**Разработчик:** `python file_utils.py` → видит демо
**Пользователь:** `import file_utils` → получает только функции

---

## Частые ошибки с Main Guard

**Ошибка #1: Одно подчёркивание**
```python
if _name_ == "__main__":  # ❌ НЕПРАВИЛЬНО!
```

**Ошибка #2: Забыли кавычки**
```python
if __name__ == __main__:  # ❌ SyntaxError!
```

**Правильно:**
```python
if __name__ == "__main__":  # ✅ ДВА подчёркивания + кавычки
```

---

## Частые ошибки (продолжение)

**Ошибка #3: Забыли двоеточие**
```python
if __name__ == "__main__"  # ❌ SyntaxError!
    main()
```

**Ошибка #4: Неправильный отступ**
```python
if __name__ == "__main__":
main()  # ❌ IndentationError!
```

**Правильно:**
```python
if __name__ == "__main__":
    main()  # ✅ 4 пробела + двоеточие!
```

---

## Правило: Когда Использовать Main Guard

**ВСЕГДА используйте в главном файле проекта:**
```python
# main.py
if __name__ == "__main__":
    main()
```

**Используйте для тестирования/демо:**
```python
# text_utils.py
def my_function():
    ...

if __name__ == "__main__":
    # Тесты функций
    print(my_function())
```
---

**НЕ используйте в простых скриптах, которые никто не импортирует:**
```python
# one_time_script.py - одноразовый скрипт
# Можно обойтись без main guard
text = input()
print(text.upper())
```

---
## ❌ Еще ошибка: Печать функции, не результата

```python
print(analyze)  # ❌ Печатает: <function analyze at 0x...>
```

**Правильно:**
```python
result = analyze(text)  # ✅ ВЫЗЫВАЕМ функцию с ()
print(result)           # ✅ Печатаем результат
```

**Или в одну строку:**
```python
print(analyze(text))  # ✅ Вызов + печать
```

---

**Context Manager (`with`):**
- Автоматически закрывает файлы
- Работает даже при ошибках
- Управляет ресурсами безопасно
- **ВСЕГДА** используйте с файлами!

**Main Guard (`if __name__ == "__main__"`):**
- Разделяет "библиотека" и "программа"
- Позволяет импортировать без выполнения кода
- Необходим в модульных проектах
- **ВСЕГДА** используйте в главном файле!


---

## Финальный проект: Анонс

**Ваша задача:** Создать профессиональный Python-проект для анализа текстового корпуса

**Что нужно сделать:**
1. Собрать **20+ текстовых файлов** (любой автор/жанр/язык)
2. Создать **структуру проекта** (как настоящий программист!)
3. Написать **код для анализа** (подсчёт слов, TTR, статистика)
4. Сохранить **результаты** (CSV + текстовый отчёт)
5. Написать **документацию** (README + docstrings)

**Дедлайн:** 20.12.2025

---

## Пример: Структура Проекта

```
dante_corpus_project/
├── corpus/              # Ваши тексты (формат .txt)
│   ├── canto_1.txt
│   ├── canto_2.txt
│   └── ...
├── data/
│   └── metadata.csv     # Информация о текстах
├── results/             # Создаётся автоматически
│   ├── statistics.csv   # Результаты анализа
│   └── report.txt       # Текстовый отчёт
├── main.py              # Главный файл - точка входа
├── text_utils.py        # Функции анализа текста
├── file_utils.py        # Функции работы с файлами
└── README.md            # Документация
```

---

# Профессиональная структура с модулями
```
project/
├── main.py       # Точка входа
├── text_utils.py # Функции анализа
└── file_utils.py # Функции I/O
```

---

## Почему нужна структура?

- Легче найти нужную функцию
- Легче исправить ошибку
- Легче добавить новую функцию
- Можно переиспользовать код в других проектах

---

## Принцип Separation of Concerns: один модуль - одна задача

**`file_utils.py`** - Работа с файлами: читать файл, записать файл, получить список файлов

**`text_utils.py`** - Анализ текста: подсчитать слова, найти уникальные слова, вычислить TTR, частотный анализ

**`main.py`** - Оркестрация: вызвать функции из других модулей, объединить результаты, вывести отчёт

---


## Архитектура проекта

```
┌─────────────────────────────────────────┐
│           main.py                       │
│  (оркестрация всей программы)           │
└────────┬────────────────────────┬───────┘
         │                        │
    ┌────▼────────┐        ┌──────▼─────┐
    │ text_utils  │        │ file_utils │
    │ (анализ)    │        │ (I/O)      │
    └─────────────┘        └────────────┘
              │                   │
              └─────────┬─────────┘
                        ▼
           ┌─────────────────────┐
           │  corpus/ (тексты)   │
           │  data/ (metadata)   │
           │  results/ (выход)   │
           └─────────────────────┘
```

---

```
my_corpus_project/
├── corpus/           # Создайте эту папку
├── data/             # Создайте эту папку
├── main.py           # Создайте этот файл
├── text_utils.py     # Создайте этот файл
└── file_utils.py     # Создайте этот файл
```

**Как создать в VS Code:**
1. File → New Folder → назовите `my_corpus_project`
2. File → Open Folder → откройте `my_corpus_project`
3. Правая кнопка в Explorer → New Folder → `corpus`
4. Правая кнопка в Explorer → New Folder → `data`
5. Правая кнопка в Explorer → New File → `main.py`

---

## Пути к файлам: относительные vs абсолютные

**Абсолютный путь** - полный путь от корня диска:
```python
# Windows
path = "C:\\Users\\Maria\\Desktop\\my_project\\corpus\\text.txt"

# macOS/Linux
path = "/Users/maria/Desktop/my_project/corpus/text.txt"
```

**Проблема:** Не работает на другом компьютере!

**Относительный путь** - путь относительно текущего файла:
```python
# Работает везде!
path = "corpus/text_1.txt"
```

---

## Модуль `os` для Работы с Путями

```python
import os

# Получить текущую директорию
current_dir = os.getcwd()
print(current_dir)  # /Users/maria/my_corpus_project

# Получить список файлов в папке
files = os.listdir("corpus")
print(files)  # ['text_1.txt', 'text_2.txt', ...]
```

---

```python
# Объединить пути (работает на Windows и macOS!)
corpus_folder = "corpus"
filename = "text_1.txt"
full_path = os.path.join(corpus_folder, filename)
# Результат: corpus/text_1.txt (или corpus\text_1.txt на Windows)

# Проверить существование
if os.path.exists(full_path):
    print("Файл существует!")
```
---

```python
with open(filepath, 'r', encoding='utf-8') as f:
  return f.read()
```

**Обратите внимание:**
- `with open(...)` - автоматически закрывает файл
- `encoding='utf-8'` - правильная кодировка

---

## Запись в текстовый файл

```python
with open(filepath, 'w', encoding='utf-8') as f:
  f.write(content)
```

**Режимы открытия файла:**
- `'r'` - Read (чтение) - файл должен существовать
- `'w'` - Write (запись) - создаёт новый файл или перезаписывает
- `'a'` - Append (добавление) - дописывает в конец файла

---

## Чтение Всех Файлов из Папки

```python
folder_path = "corpus"
files_in_folder = os.listdir(folder_path)
```

---

# Обработка Ошибок в Python

## `try` - `except` - Простой Способ

---

## Зачем Нужна Обработка Ошибок?

**Проблема:**

```python
with open('file_not_exists.txt', 'r', encoding='utf-8') as f:
    content = f.read()
```

**Результат:**
```
FileNotFoundError: [Errno 2] No such file or directory: 'file_not_exists.txt'
```

 **Программа аварийно завершается!**

---

## Решение: `try-except`

```python
try:
    with open('file_not_exists.txt', 'r', encoding='utf-8') as f:
        content = f.read()
except FileNotFoundError:
    print("Файл не найден!")
    content = ""
```

**Результат:**
```
Файл не найден!
```

**Программа продолжает работать!**

---

## Базовая структура `try-except`

```python
try:
    # Код, который МОЖЕТ вызвать ошибку
    risky_operation()
except ErrorType:
    # Что делать, если ошибка произошла
    handle_error()
```

**Как это работает:**
1. Python **пытается** выполнить код в блоке `try`
2. Если ошибка возникает → переходит в блок `except`
3. Если ошибки нет → блок `except` пропускается

---

## Пример 1: Файл не найден

**БЕЗ обработки ошибок:**

```python
def read_file_bad(filepath):
    with open(filepath, 'r', encoding='utf-8') as f:
        return f.read()

text = read_file_bad('missing.txt')  # 💥 CRASH!
```
---

**С обработкой ошибок:**

```python
def read_file_good(filepath):
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            return f.read()
    except FileNotFoundError:
        print(f"⚠️  Файл не найден: {filepath}")
        return ""

text = read_file_good('missing.txt')  # ✅ Продолжает работу
```

---

## Пример 2: Проблема с кодировкой

```python
def read_text_safe(filepath):
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            return f.read()
    except FileNotFoundError:
        print(f"Файл не найден: {filepath}")
        return ""
    except UnicodeDecodeError:
        print(f"Ошибка кодировки: {filepath}")
        return ""
```

**Можно обрабатывать несколько типов ошибок!**

---

## Типичные ошибки при работе с файлами

| Ошибка | Когда возникает | Как обработать |
|--------|----------------|----------------|
| `FileNotFoundError` | Файл не существует | Вернуть пустую строку / создать файл |
| `UnicodeDecodeError` | Неправильная кодировка | Попробовать другую кодировку |
| `PermissionError` | Нет прав доступа | Сообщить пользователю |
| `IsADirectoryError` | Путь ведёт к папке, а не файлу | Проверить тип |

---


## Пример 3: Деление на ноль

```python
def calculate_ttr(text):
    """Вычисляет Type-Token Ratio."""
    total_words = len(text.split())
    unique_words = len(set(text.split()))

    try:
        ttr = unique_words / total_words
        return ttr
    except ZeroDivisionError:
        print("Текст пустой, невозможно вычислить TTR")
        return 0
```

**Защита от пустого текста!**

---

## Пример 4: Преобразование типов

```python
def get_year_from_metadata(row):
    """Получает год из CSV строки."""
    try:
        year = int(row['year'])
        return year
    except ValueError:
        # Год не указан или указан текстом ("Unknown")
        print(f"⚠️  Не могу преобразовать год: {row['year']}")
        return None
```

---

## Универсальная Обработка: `except Exception`

```python
def safe_operation(filepath):
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            return f.read()
    except Exception as e:
        print(f"Произошла ошибка: {e}")
        return ""
```

**`Exception`** - ловит ВСЕ ошибки

**Но лучше указывать конкретные типы ошибок!**

---
### Хорошо:

```python
try:
    result = risky_operation()
except FileNotFoundError:
    handle_specific_error()
except UnicodeDecodeError:
    handle_another_error()
```

### Плохо:

```python
try:
    result = risky_operation()
except:  # Ловит ВСЁ, непонятно что пошло не так
    pass  # Игнорирует ошибку
```

---

## Полная Структура: `try-except-else-finally`

```python
try:
    # Пытаемся выполнить
    with open('file.txt', 'r', encoding='utf-8') as f:
        content = f.read()
except FileNotFoundError:
    # Если файл не найден
    print("Файл не найден")
else:
    # Выполнится, если ошибки НЕ БЫЛО
    print("Файл успешно прочитан")
finally:
    # Выполнится ВСЕГДА (есть ошибка или нет)
    print("Операция завершена")
```

**Обычно достаточно `try-except`!**

---

## Применение в Проекте

### Где использовать `try-except`:

1. **Чтение файлов** - файл может не существовать
2. **Запись файлов** - может не быть прав доступа
3. **Парсинг CSV** - данные могут быть неправильного формата
4. **Математические операции** - деление на ноль
5. **Преобразование типов** - строка → число

---

## Сравнение: с ошибками vs без ошибок

### БЕЗ обработки:
```python
corpus = {}
for filename in files:
    filepath = os.path.join('corpus', filename)
    with open(filepath, 'r', encoding='utf-8') as f:
        corpus[filename] = f.read()
```
Один битый файл → программа падает
Теряем все данные

---

### С обработкой:
```python
corpus = {}
for filename in files:
    filepath = os.path.join('corpus', filename)
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            corpus[filename] = f.read()
    except Exception as e:
        print(f"⚠️  Ошибка в {filename}: {e}")
```
Один битый файл → пропускаем его
Остальные файлы обрабатываются

---

## Минимально необходимо:

```python
def read_text_file(filepath):
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            return f.read()
    except FileNotFoundError:
        return ""
```

**Этого достаточно для базовой обработки ошибок!**

---

## Ключевые Моменты

1. **`try-except`** - защищает от аварийного завершения программы
2. **Указывайте конкретные типы ошибок** - `FileNotFoundError`, `UnicodeDecodeError`
3. **Возвращайте разумные значения** - пустая строка `""`, `None`, `0`
4. **Информируйте пользователя** - `print("Ошибка...")`
5. **Не игнорируйте ошибки полностью** - всегда обрабатывайте

---

## Практические Советы

### 1. Начните с простого
```python
try:
    # код
except Exception as e:
    print(f"Ошибка: {e}")
```
---
### 2. Уточняйте по мере необходимости
```python
try:
    # код
except FileNotFoundError:
    print("Файл не найден")
except UnicodeDecodeError:
    print("Проблема с кодировкой")
```

### 3. Тестируйте обработку ошибок
- Попробуйте открыть несуществующий файл
- Проверьте, что программа не падает
