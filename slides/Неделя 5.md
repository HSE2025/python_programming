---
marp: true
header: 
footer: ![height:40px](https://www.markslides.ai/image/credit.png)
title: ""
paginate: false
class: normal
theme: default
size: 16:9
style: |
  pre {
    overflow: auto;
  }
---

# Неделя 5 — Функции и работа с файлами

> Сегодня: **функции** как переиспользуемые инструменты + **чтение текстовых файлов** в Colab для анализа текстов.

---

# Зачем нужны функции?

**DRY** — Don't Repeat Yourself (не повторяйся)

Функция как **специализированный инструмент филолога**:
- Один инструмент — одна задача
- Переиспользование кода
- Читаемость и понятность
- Тестирование и отладка

---

# Что такое функция?

Блок кода с именем, который можно вызывать многократно

```python
def clean_text(manuscript):
    """Очищает текст от лишних пробелов"""
    return manuscript.strip().lower()
```

**Использование**: «призываем эксперта для очистки»

```python
result = clean_text("  Древний текст  ")
print(result)  # древний текст
```

---

# Анатомия функции

```python
def function_name(parameter1, parameter2):
    """Это docstring — описание функции"""
    # Тело функции
    result = parameter1 + parameter2
    return result
```

**Ключевые части**:
- `def` — ключевое слово объявления
- Имя функции (как для переменных)
- Параметры в скобках
- Двоеточие и отступ
- `return` — возврат результата

---

# Параметры и аргументы

**Параметры** — переменные в определении функции
**Аргументы** — конкретные значения при вызове

```python
def greet(name):           # name — параметр
    return f"Привет, {name}!"

message = greet("Анна")    # "Анна" — аргумент
```

---

# Параметры по умолчанию

**Настройки по умолчанию** — как стандартная конфигурация инструмента

```python
def normalize(text, preserve_case=False, remove_punct=True):
    """Нормализует текст с опциями"""
    result = text.strip()

    if not preserve_case:
        result = result.lower()

    if remove_punct:
        result = result.replace(".", "").replace(",", "")

    return result
```

---

# Использование параметров по умолчанию

```python
# Позиционные аргументы — основные данные
normalize("Текст.")
```
'текст'

```python
# Именованные аргументы — точная настройка
normalize("Текст.", preserve_case=True)
```
'Текст'

```python
normalize("Текст.", remove_punct=False)
```
'текст.'

---

# `return` vs `print`

**Важное различие**:
- `return` — передаёт результат программе (для дальнейшей работы)
- `print` — показывает информацию человеку (для отображения)

```python
def bad_function(x):
    print(x * 2)  # Только печатает!

def good_function(x):
    return x * 2  # Возвращает результат

result = bad_function(5)   # result = None
result = good_function(5)  # result = 10
```

---

# Функция может делать оба действия

```python
def analyze_text(text):
    word_count = len(text.split())
    print(f"Найдено слов: {word_count}")  # Информация для пользователя
    return word_count                      # Результат для программы

count = analyze_text("Мама мыла раму")
# Выводит: Найдено слов: 3
# count = 3
```

---

# Документация функций: docstring

**Docstring** — инструкция к инструменту

```python
def count_vowels(text):
    """
    Подсчитывает количество гласных в тексте.

    Args:
        text (str): Текст для анализа

    Returns:
        int: Количество гласных букв
    """
    vowels = "аеёиоуыэюяАЕЁИОУЫЭЮЯ"
    count = 0
    for char in text:
        if char in vowels:
            count += 1
    return count
```

---

# Type hints — спецификация типов

**Type hints** помогают понять, какие данные ожидает и возвращает функция

```python
def analyze(text: str, min_length: int = 3) -> dict:
    """
    Анализирует текст и возвращает статистику.

    Args:
        text: Анализируемый текст
        min_length: Минимальная длина слова для учёта

    Returns:
        Словарь со статистикой
    """
    words = [w for w in text.split() if len(w) >= min_length]
    return {"word_count": len(words)}
```

---

# Doctest — встроенные примеры

```python
def clean(text: str, lowercase: bool = True) -> str:
    """
    Очистить края и привести к нижнему регистру.

    >>> clean("  ПрИмер  ")
    'пример'
    >>> clean("  ПрИмер  ", lowercase=False)
    'ПрИмер'
    """
    s = text.strip()
    return s.lower() if lowercase else s

# Запуск тестов
import doctest
doctest.testmod()
```

---

# Частые ошибки с функциями

**1. Молчаливая функция** — забыли `return`

```python
def bad(x):
    x * 2  # Вычислили, но не вернули!

result = bad(5)  # result = None
```

**2. Путаница return vs print**

```python
def confusing(x):
    print(x * 2)  # Печатает, но не возвращает

result = confusing(5)  # Печатает 10, но result = None
```

---

# Область видимости

**Локальные** переменные — внутри функции
**Глобальные** переменные — снаружи

```python
x = 10  # глобальная

def example():
    x = 5  # локальная (не меняет глобальную!)
    print("Внутри:", x)

example()       # Внутри: 5
print("Снаружи:", x)  # Снаружи: 10
```

---

# Хорошие практики

**Принцип единственной ответственности**:
- Одна функция — одна задача
- `count_words()` лучше, чем `process()`

**Говорящие имена**:
- `calculate_average()` ✓
- `do_stuff()` ✗

**Документация**:
- Всегда пишите docstring
- Используйте type hints

---

**Очистка текста**:
```python
def clean_text(text: str) -> str:
    """Принимает 'грязный' текст, возвращает обработанный"""
    return text.strip().lower()
```

**Подсчёт метрик**:
```python
def count_words(text: str) -> int:
    """Анализирует текст, возвращает число"""
    return len(text.split())
```

**Поиск элементов**:
```python
def find_longest_word(text: str) -> str:
    """Ищет элемент, возвращает результат"""
    words = text.split()
    return max(words, key=len) if words else ""
```

---

# Часть 2: Работа с файлами в Colab

---

# Зачем читать файлы?

- Работа с корпусами текстов
- Анализ больших объёмов данных
- Обработка исторических документов
- Исследование литературных произведений

**Colab** — онлайн-среда, файлы эфемерны (пропадают после перезапуска)

---

# Загрузка файлов в Colab

**Способ 1**: Интерфейс слева (📁 Files)

**Способ 2**: Программная загрузка

```python
from google.colab import files

uploaded = files.upload()
filename = list(uploaded.keys())[0]
print(f'Загружен файл: {filename}')
```

**Путь**: файлы попадают в `/content/`

---

# Кодировка — критически важно!

**UTF-8** — универсальная кодировка для Unicode

```python
# Правильно:
with open('poem.txt', 'r', encoding='utf-8') as f:
    text = f.read()

# Если возникают проблемы:
with open('poem.txt', 'r', encoding='utf-8', errors='replace') as f:
    text = f.read()
```

**Другие кодировки**:
- `cp1251` — Windows (кириллица)
- `utf-16` — некоторые Word файлы
- `latin1` — западноевропейские языки

---

# Типичные ошибки при открытии файлов

**FileNotFoundError**:
```python
# Файла нет или неверный путь
open('nonexistent.txt')  # ❌
```

**UnicodeDecodeError**:
```python
# Неправильная кодировка
open('file.txt', encoding='ascii')  # ❌ для кириллицы
```

**PermissionError**:
```python
# Нет прав доступа к файлу
```

---

# Способы чтения файла

**`.read()`** — весь файл одной строкой

```python
with open('poem.txt', 'r', encoding='utf-8') as f:
    text = f.read()
print(len(text))  # количество символов
```

**`.readline()`** — одна строка за раз

```python
with open('poem.txt', 'r', encoding='utf-8') as f:
    first_line = f.readline()
    second_line = f.readline()
```

---

# Способы чтения файла

**`.readlines()`** — все строки в список

```python
with open('poem.txt', 'r', encoding='utf-8') as f:
    lines = f.readlines()  # список со \n на концах
```

**`.splitlines()`** — строки без `\n`

```python
with open('poem.txt', 'r', encoding='utf-8') as f:
    text = f.read()
lines = text.splitlines()  # список без \n
```

---

# Курсор файла

После чтения курсор находится в конце:

```python
with open('poem.txt', 'r', encoding='utf-8') as f:
    text1 = f.read()   # читает весь файл
    text2 = f.read()   # читает пустую строку!

    f.seek(0)          # вернуться в начало
    text3 = f.read()   # читает весь файл снова
```

**Решение**: открыть файл заново или использовать `seek(0)`

---

# Валидация данных

**Проверяем то, что получили**:

```python
with open(filename, 'r', encoding='utf-8', errors='replace') as f:
    text = f.read()

# Проверка на пустоту
if not text.strip():
    print("⚠️ Файл пустой!")
else:
    print(f"✓ Текст содержит {len(text)} символов")

# Проверка размера
if len(text) < 10:
    print("⚠️ Подозрительно короткий текст")
elif len(text) > 1000000:
    print("⚠️ Очень большой файл")
else:
    print("✓ Размер в разумных пределах")
```

---

# Простые метрики без циклов

```python
with open('poem.txt', 'r', encoding='utf-8') as f:
    text = f.read()

# Количество символов
char_count = len(text)

# Количество строк
line_count = len(text.splitlines())
# или
line_count = text.count('\n') + 1

# Средняя длина строки
chars_no_newlines = len(text.replace('\n', ''))
avg_length = chars_no_newlines / line_count if line_count else 0
```

---

# Превью файла

```python
with open('poem.txt', 'r', encoding='utf-8') as f:
    text = f.read()

# Первые 300 символов
print("📖 Начало файла:")
print(text[:300])

print("\n...\n")

# Последние 300 символов
print("📖 Конец файла:")
print(text[-300:])
```

---

# Окончания строк: сюрприз!

**Разные системы используют разные символы**:
- Windows: `\r\n` (CRLF)
- Unix/Linux/Mac: `\n` (LF)
- Старый Mac: `\r` (CR)

Python автоматически обрабатывает это, но иногда нужно знать:

```python
# Универсальный способ
text.splitlines()  # работает со всеми вариантами
```

---

```python
def analyze_file(filename: str) -> dict:
    """
    Анализирует текстовый файл.
    Args:
        filename: Путь к файлу
    Returns:
        Словарь с метриками
    """
    with open(filename, 'r', encoding='utf-8', errors='replace') as f:
        text = f.read()

    lines = text.splitlines()

    return {
        'chars': len(text),
        'lines': len(lines),
        'words': len(text.split()),
        'avg_line_length': len(text.replace('\n', '')) / len(lines) if lines else 0
    }
```

---

```python
def poem_analysis_from_file(filename: str):
    """Полный анализ стихотворения из файла"""

    # Читаем файл
    with open(filename, 'r', encoding='utf-8', errors='replace') as f:
        poem = f.read()

    # Анализ
    lines = poem.splitlines()
    words = clean_text(poem).split()

    # Вывод
    print(f"📖 Анализ файла: {filename}")
    print(f"📏 Строк: {len(lines)}")
    print(f"📝 Слов: {len(words)}")
    print(f"🔤 Гласных: {count_vowels(poem)}")
    print("✓ Анализ завершён")
```

---

# Шпаргалка: Функции

- **Определение**: `def name(params): ... return result`
- **Параметры по умолчанию**: `def func(x, y=10):`
- **Docstring**: `"""Описание функции"""`
- **Type hints**: `def func(x: int) -> str:`
- **Assert**: `assert condition, "Сообщение"`
- **return vs print**: return для программы, print для человека
- **Область видимости**: локальные vs глобальные переменные

---

# Шпаргалка: Файлы

- **Загрузка в Colab**: `files.upload()`
- **Открытие**: `with open(filename, 'r', encoding='utf-8') as f:`
- **Чтение**: `.read()`, `.readline()`, `.readlines()`, `.splitlines()`
- **Кодировка**: `encoding='utf-8'`, `errors='replace'`
- **Курсор**: `f.seek(0)` для возврата в начало
- **Валидация**: проверка на пустоту, размер, корректность
- **Метрики**: `len()`, `.count()`, `.splitlines()`

---

# Частые ошибки

**Функции**:
- Забыли `return` — функция возвращает `None`
- Путаница `return` vs `print`
- Изменяемые значения по умолчанию
- Слишком много обязанностей в одной функции

**Файлы**:
- Забыли указать `encoding='utf-8'`
- Не проверили существование файла
- Читаем файл дважды без `seek(0)`
- Не валидируем прочитанные данные

---

# Практические советы

**DRY (Don't Repeat Yourself)**:
- Если копируете код — создайте функцию

**Маленькие функции**:
- Лучше много маленьких, чем одна большая

**Документация**:
- Пишите docstring сразу, пока помните, что делает функция

**Тестирование**:
- Добавляйте doctest для примеров
