# Неделя 9: Знай и люби свой компьютер 💻
## Шпаргалка для студентов (Windows & macOS)

---

## 📋 Содержание

1. [Работа с терминалом/командной строкой](#работа-с-терминаломкомандной-строкой)
2. [Навигация по файловой системе](#навигация-по-файловой-системе)
3. [Проверка и запуск Python](#проверка-и-запуск-python)
4. [Работа с файлами](#работа-с-файлами)
5. [Установка VSCode](#установка-vscode)
6. [Типичные проблемы и их решение](#типичные-проблемы-и-их-решение)
7. [Горячие клавиши](#горячие-клавиши)

---

## 🖥️ Работа с терминалом/командной строкой

### Как открыть терминал/командную строку

#### Windows (Командная строка - CMD)
**Способ 1 (самый быстрый):**
- Нажмите `Win + R`
- Введите `cmd`
- Нажмите Enter

**Способ 2:**
- Нажмите `Win` (кнопка Пуск)
- Введите "cmd" или "командная строка"
- Нажмите Enter

**Способ 3:**
- Правой кнопкой на папке в Проводнике
- "Открыть в Терминале" (Windows 11)
- Или "Открыть окно PowerShell здесь" (Windows 10)

#### macOS (Терминал)
**Способ 1 (самый быстрый):**
- Нажмите `Cmd + Пробел` (Spotlight)
- Введите "Terminal"
- Нажмите Enter

**Способ 2:**
- Откройте Finder
- Перейдите в Applications → Utilities
- Дважды кликните Terminal

**Способ 3:**
- Правой кнопкой на папке в Finder
- Services → New Terminal at Folder

---

## 📁 Навигация по файловой системе

### Базовые команды

| Действие | Windows (CMD) | macOS/Linux (Terminal) | Описание |
|----------|---------------|------------------------|----------|
| **Показать текущую папку** | `cd` | `pwd` | Где я нахожусь? |
| **Список файлов** | `dir` | `ls` | Что здесь есть? |
| **Список файлов (подробно)** | `dir` | `ls -la` | Все файлы с деталями |
| **Перейти в папку** | `cd имя_папки` | `cd имя_папки` | Войти в папку |
| **Вернуться назад** | `cd ..` | `cd ..` | Выйти на уровень вверх |
| **Перейти на диск** | `D:` | — | Только для Windows |
| **Перейти в домашнюю папку** | `cd %USERPROFILE%` | `cd ~` | Моя папка |
| **Очистить экран** | `cls` | `clear` | Очистить терминал |
| **Создать папку** | `mkdir имя` | `mkdir имя` | Новая папка |
| **Удалить файл** | `del файл.txt` | `rm файл.txt` | Удалить файл |
| **Удалить папку** | `rmdir /s папка` | `rm -r папка` | Удалить папку |

### Примеры использования

#### Windows
```cmd
C:\Users\Maria>cd Documents          # Перейти в Documents
C:\Users\Maria\Documents>dir         # Показать файлы
C:\Users\Maria\Documents>mkdir python_projects  # Создать папку
C:\Users\Maria\Documents>cd python_projects     # Войти в неё
C:\Users\Maria\Documents\python_projects>cd ..  # Вернуться назад
```

#### macOS
```bash
~ $ pwd                              # Где я?
/Users/maria
~ $ cd Documents                     # Перейти в Documents
~/Documents $ ls                     # Показать файлы
~/Documents $ mkdir python_projects  # Создать папку
~/Documents $ cd python_projects     # Войти в неё
~/Documents/python_projects $ cd ..  # Вернуться назад
```

### Работа с путями с пробелами

#### Windows
```cmd
# Если в пути есть пробелы - используйте кавычки!
cd "My Documents"
cd "Python Projects\Week 9"
```

#### macOS
```bash
# Способ 1: кавычки
cd "My Documents"
cd "Python Projects/Week 9"

# Способ 2: обратный слэш перед пробелом
cd My\ Documents
cd Python\ Projects/Week\ 9
```

---

## 🐍 Проверка и запуск Python

### Проверить версию Python

#### Windows
```cmd
python --version
# Должно показать: Python 3.11.x или выше
```

#### macOS
```bash
python3 --version
# Должно показать: Python 3.11.x или выше
```

⚠️ **Важно для macOS:** Используйте `python3`, не `python` (python - это старая версия 2.x)

### Запустить Python (интерактивный режим - REPL)

#### Windows
```cmd
python
>>> print("Привет!")
Привет!
>>> exit()  # Выход из REPL
```

#### macOS
```bash
python3
>>> print("Привет!")
Привет!
>>> exit()  # Выход из REPL
```

### Запустить Python скрипт

#### Windows
```cmd
# Перейдите в папку со скриптом
cd путь\к\папке
# Запустите скрипт
python script_name.py
```

#### macOS
```bash
# Перейдите в папку со скриптом
cd путь/к/папке
# Запустите скрипт
python3 script_name.py
```

### Пример: полный процесс запуска скрипта

#### Windows
```cmd
C:\Users\Maria>cd Documents
C:\Users\Maria\Documents>cd python_projects
C:\Users\Maria\Documents\python_projects>dir
 lesson9.py
C:\Users\Maria\Documents\python_projects>python lesson9.py
Привет из скрипта!
```

#### macOS
```bash
~ $ cd Documents
~/Documents $ cd python_projects
~/Documents/python_projects $ ls
lesson9.py
~/Documents/python_projects $ python3 lesson9.py
Привет из скрипта!
```

---

## 📄 Работа с файлами

### Создать текстовый файл

#### Windows
```cmd
# В командной строке
echo Привет, мир! > test.txt

# Или используйте Блокнот
notepad test.txt  # Откроет Блокнот для редактирования
```

#### macOS
```bash
# В терминале
echo "Привет, мир!" > test.txt

# Или используйте встроенный редактор
nano test.txt  # Простой редактор
# Ctrl+X для выхода, Y для сохранения
```

### Посмотреть содержимое файла

#### Windows
```cmd
type имя_файла.txt
```

#### macOS
```bash
cat имя_файла.txt
```

### Скопировать файл

#### Windows
```cmd
copy источник.txt назначение.txt
```

#### macOS
```bash
cp источник.txt назначение.txt
```

### Переместить файл

#### Windows
```cmd
move файл.txt новая_папка\
```

#### macOS
```bash
mv файл.txt новая_папка/
```

---

## 💻 Установка VSCode

### Скачивание VSCode

#### Windows
1. Откройте браузер
2. Перейдите на https://code.visualstudio.com/
3. Нажмите "Download for Windows"
4. Запустите скачанный файл `VSCodeSetup.exe`
5. **ВАЖНО:** Поставьте галочки:
   - ☑️ "Add to PATH" (Добавить в PATH)
   - ☑️ "Register Code as an editor" (Зарегистрировать как редактор)
   - ☑️ "Add 'Open with Code'" (Добавить "Открыть с помощью Code")
6. Нажмите "Install"

#### macOS
1. Откройте браузер
2. Перейдите на https://code.visualstudio.com/
3. Нажмите "Download for Mac"
4. Откройте скачанный файл `.dmg`
5. Перетащите VSCode в папку Applications
6. Откройте VSCode из Applications

**Опционально (добавить команду `code` в терминал):**
```bash
# В VSCode нажмите Cmd+Shift+P
# Введите "shell command"
# Выберите "Install 'code' command in PATH"
```

### Установка Python расширения в VSCode

**Для Windows и macOS одинаково:**
1. Откройте VSCode
2. Нажмите на иконку Extensions (квадратики слева)
   - Или нажмите `Ctrl+Shift+X` (Windows)
   - Или нажмите `Cmd+Shift+X` (macOS)
3. В поисковой строке введите "Python"
4. Найдите расширение от Microsoft (автор: Microsoft)
5. Нажмите "Install"

### Выбор интерпретатора Python в VSCode

1. Откройте VSCode
2. Откройте любой `.py` файл
3. Нажмите на панели внизу там где указан Python
4. Или нажмите `Ctrl+Shift+P` / `Cmd+Shift+P`
5. Введите "Python: Select Interpreter"
6. Выберите Python 3.11+ из списка

---

## 🔧 Типичные проблемы и их решение

### Проблема 1: "python is not recognized" (Windows)

**Симптом:**
```cmd
C:\Users\Maria>python --version
'python' is not recognized as an internal or external command
```

**Причина:** Python не добавлен в PATH

**Решение 1 (переустановка - рекомендуется):**
1. Удалите Python через "Установка и удаление программ"
2. Скачайте новый установщик с python.org
3. При установке **ОБЯЗАТЕЛЬНО** поставьте галочку:
   ☑️ **"Add Python to PATH"** (внизу окна установки)
4. Нажмите "Install Now"
5. Перезапустите командную строку

**Решение 2 (добавить PATH вручную):**
1. Найдите где установлен Python (обычно `C:\Users\ИМЯ\AppData\Local\Programs\Python\Python311\`)
2. Откройте "Система" → "Дополнительные параметры системы"
3. "Переменные среды" → "Path" → "Изменить"
4. "Создать" → Вставьте путь к Python
5. Добавьте также путь к `Scripts` (например `C:\Users\ИМЯ\AppData\Local\Programs\Python\Python311\Scripts\`)
6. Нажмите OK
7. **Перезапустите командную строку**

### Проблема 2: "command not found: python" (macOS)

**Симптом:**
```bash
$ python --version
command not found: python
```

**Причина:** На macOS команда называется `python3`, не `python`

**Решение:**
Используйте `python3` вместо `python`:
```bash
python3 --version
python3 script.py
```

**Опционально (создать алиас):**
```bash
# Добавить в ~/.zshrc (macOS Catalina+) или ~/.bash_profile
echo "alias python=python3" >> ~/.zshrc
source ~/.zshrc
```

### Проблема 3: FileNotFoundError при чтении файла

**Симптом:**
```python
FileNotFoundError: [Errno 2] No such file or directory: 'my_text.txt'
```

**Причина:** Файл не находится в той же папке, что и скрипт

**Решение:**
```python
# Способ 1: Убедитесь что файл в той же папке
# В терминале:
ls          # macOS - должен показать и .py и .txt файл
dir         # Windows - должен показать и .py и .txt файл

# Способ 2: Используйте полный путь
# Windows:
filename = r"C:\Users\Maria\Documents\my_text.txt"

# macOS:
filename = "/Users/maria/Documents/my_text.txt"

# Способ 3: Проверьте текущую папку в коде
import os
print(os.getcwd())  # Где находится программа
```

### Проблема 4: Кракозябры вместо русских букв

**Симптом:**
```
РџСЂРёРІРµС‚!
```

**Причина:** Неправильная кодировка файла

**Решение:**
```python
# ВСЕГДА указывайте encoding='utf-8' при работе с файлами!
with open('my_text.txt', 'r', encoding='utf-8') as file:
    content = file.read()
    
# Также при записи:
with open('output.txt', 'w', encoding='utf-8') as file:
    file.write("Привет!")
```

### Проблема 5: VSCode не видит Python

**Симптом:** В VSCode снизу написано "Python not found" или не запускается код

**Решение:**
1. Убедитесь что Python установлен (проверьте в терминале: `python --version`)
2. Установите Python Extension в VSCode
3. Выберите интерпретатор:
   - Нажмите `Ctrl+Shift+P` (Windows) / `Cmd+Shift+P` (macOS)
   - Введите "Python: Select Interpreter"
   - Выберите Python 3.11+ из списка
4. Перезапустите VSCode

### Проблема 6: VSCode открывается, но нет терминала

**Решение:**
- Нажмите `Ctrl+` ` (Windows) - символ обратной кавычки
- Или `Cmd+` ` (macOS)
- Или через меню: Terminal → New Terminal
- Или через меню: View → Terminal

### Проблема 7: Не могу найти скачанный файл

**Windows:**
1. Откройте проводник
2. Слева нажмите "Загрузки" (Downloads)
3. Найдите файл по дате или имени

**macOS:**
1. Откройте Finder
2. Слева нажмите "Downloads"
3. Найдите файл по дате или имени

### Проблема 8: Скрипт запускается и сразу закрывается (Windows)

**Симптом:** Окно появляется и моментально исчезает

**Причина:** Программа завершилась слишком быстро

**Решение:**
```python
# Добавьте в конец программы:
input("Нажмите Enter для выхода...")

# Или запускайте через командную строку:
# 1. Откройте CMD
# 2. Перейдите в папку со скриптом
# 3. python script.py
```

---

## ⌨️ Горячие клавиши

### Терминал/Командная строка

| Действие | Windows | macOS |
|----------|---------|-------|
| **Автодополнение** | Tab | Tab |
| **Предыдущая команда** | ↑ | ↑ |
| **Следующая команда** | ↓ | ↓ |
| **Очистить экран** | `cls` | `clear` или Cmd+K |
| **Прервать выполнение** | Ctrl+C | Ctrl+C |
| **Копировать** | Ctrl+C (выделить текст) | Cmd+C |
| **Вставить** | Ctrl+V или правая кнопка | Cmd+V |
| **Выход** | `exit` | `exit` или Cmd+Q |

### VSCode

| Действие | Windows | macOS |
|----------|---------|-------|
| **Командная палитра** | Ctrl+Shift+P | Cmd+Shift+P |
| **Открыть файл** | Ctrl+O | Cmd+O |
| **Сохранить** | Ctrl+S | Cmd+S |
| **Сохранить всё** | Ctrl+K S | Cmd+Alt+S |
| **Закрыть файл** | Ctrl+W | Cmd+W |
| **Открыть терминал** | Ctrl+` | Cmd+` |
| **Новый терминал** | Ctrl+Shift+` | Cmd+Shift+` |
| **Запустить код** | Ctrl+Alt+N | Cmd+Alt+N |
| **Комментарий** | Ctrl+/ | Cmd+/ |
| **Найти** | Ctrl+F | Cmd+F |
| **Заменить** | Ctrl+H | Cmd+H |
| **Поиск файла** | Ctrl+P | Cmd+P |
| **Закрыть VSCode** | Alt+F4 | Cmd+Q |
| **Форматировать код** | Shift+Alt+F | Shift+Opt+F |
| **Дублировать строку** | Shift+Alt+↓ | Shift+Opt+↓ |
| **Удалить строку** | Ctrl+Shift+K | Cmd+Shift+K |
| **Переместить строку** | Alt+↑/↓ | Opt+↑/↓ |

---

## 📊 Быстрая справка: Какую команду использовать?

### Я хочу узнать...

| Что нужно узнать | Windows | macOS |
|------------------|---------|-------|
| В какой папке я сейчас? | `cd` | `pwd` |
| Какие файлы здесь есть? | `dir` | `ls` |
| Установлен ли Python? | `python --version` | `python3 --version` |
| Какой путь к Python? | `where python` | `which python3` |
| Мой IP адрес | `ipconfig` | `ifconfig` |

### Я хочу сделать...

| Что нужно сделать | Windows | macOS |
|-------------------|---------|-------|
| Перейти в другую папку | `cd путь\к\папке` | `cd путь/к/папке` |
| Создать папку | `mkdir имя` | `mkdir имя` |
| Создать файл | `notepad файл.txt` | `touch файл.txt` |
| Запустить Python скрипт | `python файл.py` | `python3 файл.py` |
| Открыть REPL | `python` | `python3` |
| Очистить экран | `cls` | `clear` |
| Остановить программу | Ctrl+C | Ctrl+C |

---

## 🆘 Экстренная помощь

### Я полностью запутался, с чего начать?

**Шаг 1: Откройте терминал**
- Windows: `Win+R` → `cmd` → Enter
- macOS: `Cmd+Пробел` → `Terminal` → Enter

**Шаг 2: Проверьте Python**
- Windows: `python --version`
- macOS: `python3 --version`

**Шаг 3: Создайте рабочую папку**
```cmd
# Windows
cd Documents
mkdir python_projects
cd python_projects

# macOS
cd Documents
mkdir python_projects
cd python_projects
```

**Шаг 4: Создайте тестовый файл**
```python
# Windows
notepad test.py
# Вставьте: print("Работает!")
# Сохраните (Ctrl+S) и закройте

# macOS
echo 'print("Работает!")' > test.py
```

**Шаг 5: Запустите**
```cmd
# Windows
python test.py

# macOS
python3 test.py
```

Если увидели "Работает!" — всё хорошо! 🎉

---

## 📚 Полезные ресурсы

### Документация
- **Python:** https://docs.python.org/3/
- **VSCode:** https://code.visualstudio.com/docs
- **VSCode Python:** https://code.visualstudio.com/docs/python/python-tutorial

### Справочники по терминалу
- **Windows CMD:** https://ss64.com/nt/
- **macOS Terminal:** https://ss64.com/osx/

### Видео-туториалы
- Как пользоваться терминалом (YouTube)
- VSCode для начинающих (YouTube)
- Python для новичков (YouTube)

---

## ✅ Чек-лист готовности к занятию

Перед началом занятия убедитесь:

- [ ] Python установлен и работает (`python --version` / `python3 --version`)
- [ ] Умею открывать терминал/командную строку
- [ ] Умею перемещаться между папками (`cd`)
- [ ] Умею смотреть содержимое папки (`dir` / `ls`)
- [ ] VSCode установлен (если ещё нет - установим на занятии)
- [ ] Знаю где находится папка "Загрузки"
- [ ] Умею создавать текстовые файлы
- [ ] Эта шпаргалка сохранена и легко доступна

---

## 🎯 Главные правила

### 1. Всегда знайте, где вы находитесь
```cmd
# Windows
cd

# macOS
pwd
```

### 2. Всегда используйте encoding='utf-8' для файлов
```python
with open('файл.txt', 'r', encoding='utf-8') as f:
    content = f.read()
```

### 3. Файлы должны быть в той же папке, что и скрипт
```cmd
# Проверьте:
dir      # Windows
ls       # macOS
```

### 4. На macOS используйте python3, не python
```bash
python3 --version
python3 script.py
```

### 5. Перезапускайте терминал после установки
После установки Python или изменения PATH **обязательно** закройте и откройте терминал заново!

---

## 💡 Лайфхаки

### Быстрое открытие терминала в нужной папке

**Windows 11:**
- Правой кнопкой на папке → "Открыть в Терминале"

**Windows 10:**
- Shift + Правая кнопка на папке → "Открыть окно PowerShell здесь"

**macOS:**
- Перетащите папку на иконку Terminal в Dock
- Или: Services → New Terminal at Folder

### Автодополнение команд
- Начните печатать имя файла/папки
- Нажмите **Tab**
- Терминал сам допишет имя!

### История команд
- Нажимайте **↑** чтобы вернуться к предыдущим командам
- Не нужно печатать одно и то же снова!

### Копирование из терминала

**Windows:**
1. Выделите текст мышкой
2. Нажмите Enter или Ctrl+C
3. Вставьте: Ctrl+V или правая кнопка

**macOS:**
1. Выделите текст
2. Cmd+C
3. Вставьте: Cmd+V

---

**Версия:** 1.0  
**Дата:** 2025-11-07  
**Курс:** Неделя 9 - Знай и люби свой компьютер  
**Язык:** Русский

**Совет:** Распечатайте эту шпаргалку или держите открытой на втором экране! 📖
